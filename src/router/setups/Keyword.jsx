import Features from '../../pages/setups/keyword/Keyword'
const KeywordRoutes = () => {
  const routes = [
    {
      path: '/keyword',
      element: <Features />,
    },
  ]
  return routes
}

export default KeywordRoutes
