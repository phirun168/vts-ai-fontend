import Area from '../../pages/Setups/area/Area'
const AreaRoutes = () => {
  const routes = [
    {
      path: '/area',
      element: <Area />,
    },
  ]
  return routes
}

export default AreaRoutes
