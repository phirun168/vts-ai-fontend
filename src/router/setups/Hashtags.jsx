import HashTags from '../../pages/setups/hashtags/Hashtags'
const HashtagsRoutes = () => {
    const routes = [
        {
            path: '/hashtags',
            element: <HashTags />,
        },
    ]
    return routes
}

export default HashtagsRoutes
